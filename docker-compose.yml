version: '3.8'

services:
  solr:
    image: solr:9.4
    container_name: ev_solr
    ports:
      - "8983:8983"
    volumes:
      - solr_data:/var/solr
    # Automatically create a core named 'ev_reviews' on startup
    command:
      - solr-precreate
      - ev_reviews

volumes:
  solr_data: